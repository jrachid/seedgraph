# seedgraph

> Seed your SQLAlchemy models as a referentially-consistent graph — one call, shared parents, real PKs.

**seedgraph fills your test database with a coherent graph of objects, in a single call.** You declare what you want — "3 users, each with 2 posts, each post with 3 comments" — and the library builds everything: the objects, the links between them, foreign keys that always point at real rows that exist (never phantom data — the invariant the library guarantees). The values are realistic and reproducible — "Jose Bishop" instead of "user-0" — generated by Faker with a fixed seed, and you can override the value of any specific object or the way any column is generated. The whole thing plugs into pytest as a fixture, with no configuration: a test can start from an empty database and get its world in two lines.

**Status: work in progress.** The API below is the target design. Everything marked 🎯 is planned, not shipped yet.

---

## Why

Every Python team that seeds a relational test database eventually hand-rolls the same
plumbing: generate rows, stage commits so primary keys exist, chase those PKs into FK
columns, repeat for every relationship, pray the graph stays consistent.

You shouldn't have to. But after empirically testing the landscape (SQLAlchemy 2.x era,
August 2026), none of the existing options actually does it:

| Tool | What you get on a `User ← Post ← Comment` schema |
|------|---------------------------------------------------|
| `polyfactory` | Builds related objects, **but every FK column is a random int pointing at nothing**: `post.author_id != post.author.id`. Silent data corruption — tests pass on garbage unless you enable FK enforcement (most don't). |
| `faker-sqlalchemy` (unmaintained since 2022, pinned to SQLAlchemy 1.x) | `RecursionError` on standard `backref` relationships, on self-referential FKs, and its `overrides` API silently drops FK values. |
| `sqlalchemyseed` | Seeds data *you already have* (JSON/YAML), doesn't generate. |
| `sqlseed`, `sowdb` | Solid fillers, but schema-level and flat: "N rows per table". They work from raw SQL schemas, not your models, and can't express a graph shape like *"3 users → 2 posts each → 5 comments per post"*. |

The one-line failure we're fixing:

```python
p = PostFactory.build()
p.author_id == p.author.id   # False. Every FK in the graph is disconnected.
```

`seedgraph` exists so this is always `True`.

### "But other libraries do this too, don't they?"

Good question, and the honest answer starts with "yes, libraries exist" — otherwise there would be nothing to challenge. The real question is: do they do the **same**? Three differences survive a closer look:

**1. A guaranteed integrity check, not just object creation.** factory_boy (and pytest-factoryboy, mixer...) create linked objects — but if you wire two factories that reference each other, or an object with a FK pointing nowhere, they create the graph **and stop there**: the phantom link ships to prod with the test. seedgraph doesn't just build, it **guarantees**: after the seed, every foreign key in the graph points at a primary key in the same graph, proven by a verification pass. It's an exit contract, not an entry convention.

**2. Coexistence with an already-populated database.** Existing libraries assume empty databases; on a database with existing data, they collide on IDs one time out of two. seedgraph reads the existing ID maxima **before** seeding and reserves above them — a test against a partially-populated environment just works, no workarounds.

**3. Seed-determinism, wired into a fixture helper.** Realistic Faker values exist (Faker gives them); deterministic end-to-end on a fresh base **and** coexisting with pre-existing rows, inside a two-line pytest fixture — that isn't assembled anywhere else.

In two sentences if you push back: *"The others manufacture objects. seedgraph manufactures a graph **whose coherence is a verified promise**, even on top of data that was already there — and it makes it reproducible for CI. It's the 'one call' + verified-coherence pairing that object-creating libraries don't put on the table."*

## The pitch 🎯

Declare a **shape**, get a **coherent object graph** in one call — FK columns provably
pointing at real PKs in the same graph, before any flush happens.

```python
from seedgraph import seed

graph = seed(
    session,
    User,
    post=2,            # 2 posts per user — shared parent
    post__comment=5,   # 5 comments per post
)
assert len(graph.users) == 3
assert graph.users[0].posts[0].author_id == graph.users[0].id  # real PK, real FK
```

## Design principles

1. **Model-first, not schema-first.** Works from your SQLAlchemy ORM models and relationships — introspection only as a fallback.
2. **Referential consistency is an invariant, not a hope.** FK reconciliation happens in the object graph, before the unit of work flushes.
3. **Shared parents are the point.** Realistic data shares parents (one author, many posts). One object per FK is not a graph.
4. **Deterministic.** A seed produces the same graph twice.
5. **Cycles and self-references are normal.** `Category.parent`, circular FKs — supported by construction.
6. **Stop generating at the boundary.** Existing rows with real PKs are usable as parents; only missing parents get generated.

## Roadmap 🎯

- [x] Core: FK-graph topology from metadata (topological order, cycle detection)
- [x] PK reservation/reconciliation across the graph
- [x] Shape API (`relation=n`, nesting, shared parents)
- [x] Custom field generators (Faker under the hood)
- [x] Overriding specific attributes on generated objects
- [x] pytest fixture helpers
- [x] Self-referential and cyclic FKs
- [x] Async sessions support

## Installation 🎯

```bash
pip install seedgraph
```

Requires Python 3.11+ and SQLAlchemy 2.x.

## License

MIT
